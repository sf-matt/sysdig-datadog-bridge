apiVersion: v1
kind: Service
metadata:
  name: sysdig-datadog-bridge
  namespace: sysdig-datadog-bridge
spec:
  type: LoadBalancer
  selector:
    app: sysdig-datadog-bridge
  ports:
    - name: http
      port: 80
      targetPort: 8080